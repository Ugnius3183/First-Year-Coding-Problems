#!/usr/bin/env python3

with open("irish-dobs.txt") as f:
    n = f.read()
i = 0
#while i < len(n):
    #m = n[i].split("/")
    #tmp = m[0]
    #m[0] = m[1]
    #m[1] = tmp
print(n)
    #i = i + 1
